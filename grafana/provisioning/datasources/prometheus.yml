apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: true

  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    user: demobot
    secureJsonData:
      password: demobot
    jsonData:
      database: demobot
      sslmode: disable
    editable: true
