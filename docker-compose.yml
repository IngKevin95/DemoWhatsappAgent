services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: demobot
      POSTGRES_PASSWORD: demobot
      POSTGRES_DB: demobot
    ports:
      - "5441:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U demobot -d demobot"]
      interval: 5s
      timeout: 5s
      retries: 10

  nocodb:
    image: nocodb/nocodb:latest
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      NC_DB: "pg://postgres:5432?u=demobot&p=demobot&d=nocodb"
      NC_ADMIN_EMAIL: "admin@democorp.com"
      NC_ADMIN_PASSWORD: "demobotpassword123"
    volumes:
      - ncdata:/usr/app/data

  seed:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    command: ["python", "scripts/seed_db.py"]
    restart: "no"

  demobot:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
      seed:
        condition: service_completed_successfully
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # ponytail: sin este cap, workers=cpu*2+1 (=65 en host de 32 cores) abre
      # más pools que max_connections de Postgres (100) -> "too many clients".
      GUNICORN_WORKERS: ${GUNICORN_WORKERS:-4}
      ESPOCRM_URL: http://espocrm
      ESPOCRM_API_KEY: ${ESPOCRM_API_KEY:-}
      FIREBIRD_HOST: firebird
    networks:
      - default
      - democorp-demo
    volumes:
      - ./token.json:/app/token.json

networks:
  default:
  democorp-demo:
    external: true

volumes:
  pgdata:
  ncdata:
