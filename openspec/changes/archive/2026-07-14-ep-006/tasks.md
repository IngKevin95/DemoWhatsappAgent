# Tareas de Implementación

- [ ] Modificar `agent/db.py` para agregar la clase `Conversacion` con relaciones.
- [ ] Modificar `agent/db.py` en `Contacto` agregando la columna `consentimiento`.
- [ ] Modificar `agent/memory.py` para agregar `abrir_conversacion(telefono)`, `cerrar_conversacion(id)`.
- [ ] Escribir tests unitarios (TDD) para estos helpers de memoria y DB.
- [ ] Actualizar `build-state.json` en fase `tdd` o `smoke` cuando corresponda.
